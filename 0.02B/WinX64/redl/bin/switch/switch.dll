	if(codest0[0] == 's') {
		if(codest0[1] == 'w') {
			if(codest0[2] == 'i') {
				if(codest0[3] == 't') {
					if(codest0[4] == 'c') {
						if(codest0[5] == 'h') {
	
							string switche;
							for(int x = 6;x<1024;x++) {
								if(codest0[x] == ':') {
									break;
								}
								switche += codest0[x]; 
							}
							out << "switch(" << switche << "){" << '\n';

						}
					}
				}
			}
		}
	}
	
	
	
	if(codest0[0] == 'c') {
		if(codest0[1] == 'a') {
			if(codest0[2] == 's') {
				if(codest0[3] == 'e') {
	
							string casers;
							for(int x = 6;x<1024;x++) {
								if(code[voice][x] == ':') {
									break;
								}
								casers += code[voice][x]; 
							}
							out << "case " << casers << " :" << '\n';

				}
			}
		}
	}


	if(codest0[0] == 'd') {
		if(codest0[1] == 'e') {
			if(codest0[2] == 'f') {
				if(codest0[3] == 'a') {
					if(codest0[4] == 'u') {
						if(codest0[5] == 'l') {
							if(codest0[6] == 't') {
								
								out << "default :" << '\n';

							}
						}
					}
				}
			}
		}
	}
