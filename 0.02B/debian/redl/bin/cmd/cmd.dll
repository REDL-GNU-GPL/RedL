if(codest0[0] == 'c') {
	if(codest0[1] == 'o') {
		if(codest0[2] == 'n') {
			if(codest0[3] == 's') {
				if(codest0[4] == 'o') {
					if(codest0[5] == 'l') {
					
						string cons;
							for(int x = 6;x<1024;x++) {
								if(codest0[x] == ';') {
									break;
								}
								cons += codest0[x]; 
							}
							out << "system(" << cons << ");" << '\n';
					
					}
				}
			}
		}
	}
}
