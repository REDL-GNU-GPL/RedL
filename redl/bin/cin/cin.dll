	if(codest0[0] == 'i')	 {
		if(codest0[1] == 'n') {
			if(codest0[2] == 'p') {
				if(codest0[3] == 'u') {
					if(codest0[4] == 't') {
						if(codest0[5] == '_') {
							if(codest0[6] == 'g') {
								if(codest0[7] == 'e') {
									if(codest0[8] == 't') {
									
										string var;
										string text;
										int x = 9;
										for(;x<1024;x++) {
											if(codest0[x] == ',') {
												break;
											}
											var += codest0[x]; 
										}
										x+=1;
										for(;x<1024;x++) {
											if(codest0[x] == ';') {
												break;
											}
											text += codest0[x]; 
										}
										out << "cout << " << text << ";" << '\n';
										out << "getline(cin," << var << ");" << '\n';
									}
								}
							}
						}
					}
				}
			}
		}
	}
	
	
	
	if(codest0[0] == 'i')	 {
		if(codest0[1] == 'n') {
			if(codest0[2] == 'p') {
				if(codest0[3] == 'u') {
					if(codest0[4] == 't') {
						if(codest0[5] == '_') {
							if(codest0[6] == 'c') {
								if(codest0[7] == 'i') {
									if(codest0[8] == 'n') {
									
										string var;
										string text;
										int x = 9;
										for(;x<1024;x++) {
											if(codest0[x] == ',') {
												break;
											}
											var += codest0[x]; 
										}
										x+=1;
										for(;x<1024;x++) {
											if(codest0[x] == ';') {
												break;
											}
											text += codest0[x]; 
										}
										out << "cout << " << text << ";" << '\n';
										out << "cin >> " << var << ";" << '\n';
									}
								}
							}
						}
					}
				}
			}
		}
	}
