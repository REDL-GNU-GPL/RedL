	if(codest0[0] == 's') {
		if(codest0[1] == 't') {
			if(codest0[2] == 'r') {
				if(codest0[3] == 'i') {
					if(codest0[4] == 'n') {
						if(codest0[5] == 'g') {
						
							string str;
							for(int x = 6;x<1024;x++) {
								if(codest0[x] == ';') {
									break;
								}
								str += codest0[x]; 
							}
							out << "std::string " << str << ";" << '\n';
						
						}
					}
				}
			}
		}
	}
