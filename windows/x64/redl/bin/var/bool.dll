	if(codest0[0] == 'b') {
		if(codest0[1] == 'o') {
			if(codest0[2] == 'o') {
				if(codest0[3] == 'l') {
	
							string boolean;
							for(int x = 4;x<1024;x++) {
								if(codest0[x] == ';') {
									break;
								}
								boolean += codest0[x]; 
							}
							out << "bool " << boolean << ";" << '\n';
							
				}
			}
		}
	}
